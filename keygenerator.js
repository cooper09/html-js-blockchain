// install some kind of key generator

function keyPair() {
    console.log("generate keyPair")
    const keyObj = {
        privateKey: "0x0",
        publicMey: "0x1"
    }
    return keyObj
}
//module.exports.keyPair = keyPair;